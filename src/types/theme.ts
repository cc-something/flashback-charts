export interface DecadeTheme {
  name: string
  fontFamily: string
  fontUrl: string
  colors: {
    background: string
    surface: string
    primary: string
    secondary: string
    text: string
    textMuted: string
    accent: string
    tabActive: string
    tabInactive: string
  }
}

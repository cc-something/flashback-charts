# Flashback Charts Australia — Apache config for cPanel hosting.
# vite-ssg prerenders /index.html, /au/index.html, /au/YYYY/index.html, etc.
# Serve prerendered files directly; fall back to /index.html for any unmatched
# path so deep links and refreshes work.

Options -MultiViews
RewriteEngine On

# Temporary homepage redirect until a global chart exists.
RewriteRule ^$ /au/ [R=302,L]

# Skip rewrite for real files and directories (assets, prerendered HTML, sitemap, etc.).
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fall back to the root SPA entry for everything else.
RewriteRule ^ index.html [L]

# Long-cache hashed assets.
<IfModule mod_headers.c>
  <FilesMatch "\.(js|css|woff2|woff|ttf|svg|png|jpg|jpeg|webp|avif)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  <FilesMatch "\.(html|xml|txt)$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>
</IfModule>

# Gzip text responses.
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml application/javascript application/json image/svg+xml
</IfModule>

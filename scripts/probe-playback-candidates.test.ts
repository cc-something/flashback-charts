import { describe, expect, it } from 'vitest'

describe('probe-playback-candidates placeholder', () => {
  it('keeps vitest discovering the script namespace without runtime probes', () => {
    expect(true).toBe(true)
  })
})
